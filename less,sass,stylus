sass设函数

less可以设全局类，实现类似sass的效果
如：

unit转为rem单位
.fs(@px) {
  font-size: unit(@px / 37.5, rem);
}
.w(@px) {
  width: unit(@px / 37.5, rem);
}
.h(@px) {
  height: unit(@px / 37.5, rem);
}

在sass,less文件里
@import 'xxx'其它less文件

sass常用的是scss文件